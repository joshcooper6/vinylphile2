    // function renderTaxByCost(state, totalCost) {
    //     const taxRate = getSalesTaxRates()?.filter(item => item.state === state)[0]?.rate;
    //     if (taxRate) { return (totalCost * taxRate).toFixed(2)};
    //     if (state === '') { return 0};
    //     return (totalCost * taxRate).toFixed(2);
    // };

    // const totalTaxes = renderTaxByCost(state, totalCartCost);
    // const grandTotal = (Number(totalCartCost) + Number(totalTaxes)).toFixed(2); 
